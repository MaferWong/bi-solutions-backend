using BISolutions.Models;

namespace BISolutions.DomainService
{
    public class Rol_UsuarioDomainService
    {/*
        public string GetRolUsuarioDomainService(int id, Rol_Usuario rolUsuario)
        {
            if (rolUsuario == null)
            {
                return "Esta asignación no existe.";
            }
            return null;
        }
        public string PostRolUsuarioDomainService(Rol_Usuario rolUsuario)
        {
            if (rolUsuario.rol_id == 0)
            {
                return "Se necesita el identificador del rol que desea asignar.";
            }
            if (rolUsuario.usuario_id == 0)
            {
                return "Se necesita el identificador del usuario al que desea asignar un rol.";
            }
            return null;
        }
        public string PutRolUsuarioDomainService(int id, Rol_Usuario rolUsuario, Rol rol, Usuario usuario)
        {
            if (id != rolUsuario.rol_usuario_id)
            {
                return "Esta asignación no existe.";
            }
            if (rol == null)
            {
                return "El rol no existe.";
            }
            if (usuario == null)
            {
                return "El usuario no existe.";
            }
            return null;
        }
        public string DeleteRolUsuarioDomainService(int id, Rol_Usuario rolUsuario)
        {
            if (rolUsuario == null)
            {
                return "Esta asignación no existe.";
            }
            return null;
        }*/
    }
}
